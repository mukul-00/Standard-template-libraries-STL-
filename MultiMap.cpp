#include <bits/stdc++.h>
using namespace std;

int explainMultiMap(){
    // everything is same as map, only it can store multiple keys
    // only map[keys] cannot be stored used here

    return 0;
}
